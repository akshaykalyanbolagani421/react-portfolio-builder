import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import { ThemeProvider } from "@/components/theme-provider"
import { Suspense } from "react"
import "./globals.css"

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
})

export const metadata: Metadata = {
  title: "Akshay Kalyan - MERN Stack Developer | CSE Student",
  description:
    "Personal portfolio of Akshay Kalyan - MERN Stack Developer and Computer Science Engineering student showcasing projects, skills, and experience.",
  generator: "v0.app",
  keywords: ["MERN Stack Developer", "React Developer", "Full Stack Developer", "Computer Science", "Portfolio"],
  authors: [{ name: "Akshay Kalyan" }],
  viewport: "width=device-width, initial-scale=1",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`font-sans ${inter.variable} antialiased`}>
        <Suspense fallback={null}>
          <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
            {children}
          </ThemeProvider>
        </Suspense>
        <Analytics />
      </body>
    </html>
  )
}
